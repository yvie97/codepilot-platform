services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB:       codepilot
      POSTGRES_USER:     codepilot
      POSTGRES_PASSWORD: codepilot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
